# torch CPU-only: lo stack CUDA (~2.3 GB) non serve, gli embedding della
# knowledge base si calcolano benissimo su CPU. Installare con:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
streamlit>=1.30.0
requests>=2.31.0
mysql-connector-python>=8.0.0
python-dotenv>=1.0.0
sentence-transformers>=2.2.0
chromadb>=0.4.0
torch>=2.0.0

# Backend API (migrazione da Streamlit a React)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Test automatici
pytest>=8.0.0
httpx>=0.27.0
